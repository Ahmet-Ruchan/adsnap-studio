# In your own Terminal
echo "streamlit==1.32.0
requests==2.31.0
python-dotenv==1.0.1
Pillow==10.0.1" > requirements.txt

git add requirements.txt
git commit -m "Downgrade Pillow to fix Streamlit Cloud deployment"
git push
